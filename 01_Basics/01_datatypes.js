"use strict"; // treat all the js code as newer version 


let name="Krishnan"
let age=20
let caste=null
let state;
let gf=true

console.table([typeof(name), typeof(age), typeof(caste), typeof(state)])